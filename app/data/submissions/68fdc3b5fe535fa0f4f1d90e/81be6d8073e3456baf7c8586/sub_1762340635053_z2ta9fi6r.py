import sys
def mat_ma_kaelen():
T=sys.stdin.readline().strip() 
t=int(T)
for _ in range(t):
S=sys.stdin.readline().strip()
product=1
sum_even=0
has_non_zero_odd=False
for i in range(len(s)):
    digit=int(s[i])
    if(i+1)%2==1
      if digit!=0:
        product *= digit
            has_non_zero_odd = True
    else:
        sum_even += digit
        if not has_non_zero_odd:
    product = 0
    print(product, sum_even)
        